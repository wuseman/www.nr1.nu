# pdftotext

##### GREP a PDF file.

   pdftotext  [file] - | grep 'YourPattern'

##### Count words in a TeX/LaTeX document.

   pdftotext  file.pdf - | wc -w

##### GREP a PDF file.

   pdftotext  [file] - | grep 'YourPattern'

##### Count words in a TeX/LaTeX document.

   pdftotext  file.pdf - | wc -w
