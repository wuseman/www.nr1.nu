# asterisk

##### Watch active calls on an Asterisk PBX

   asterisk  -rx "core show calls" | grep "active" | cut -d' ' -f1

##### Watch active calls on an Asterisk PBX

   asterisk  -rx "core show calls" | grep "active" | cut -d' ' -f1
