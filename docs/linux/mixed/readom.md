# readom

##### Create a CD/DVD ISO image from disk.

   readom  dev=/dev/scd0 f=/path/to/image.iso

##### Create a CD/DVD ISO image from disk.

   readom  dev=/dev/scd0 f=/path/to/image.iso

##### Create a CD/DVD ISO image from disk.

   readom  dev=/dev/scd0 f=/path/to/image.iso
