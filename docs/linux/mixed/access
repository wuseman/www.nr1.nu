# access

##### security find-generic-password -ga "ROUTERNAME" | grep "password:"

   access  wifi password through terminal (osx)

##### security find-generic-password -ga "ROUTERNAME" | grep "password:"

   access  wifi password through terminal (osx)
