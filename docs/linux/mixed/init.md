# init

##### sun solaris 9 complete restart

   init  6

##### password recovery on debian

   init =/bin/bash; mount -o remount,rw /

##### password recovery on debian

   init =/bin/bash; mount -o remount,rw /
