# pbc

##### Apply, in parallel, a bc expression to CSV

   pbc  () { parallel -C, -k -j100% "echo '$@' | bc -l"; }

##### Apply, in parallel, a bc expression to CSV

   pbc  () { parallel -C, -k -j100% "echo '$@' | bc -l"; }
