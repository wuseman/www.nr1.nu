# users

##### List and count the number of open sessions per user

   users  | xargs -n1 echo | sort | uniq -c

##### List and count the number of open sessions per user

   users  | xargs -n1 echo | sort | uniq -c
