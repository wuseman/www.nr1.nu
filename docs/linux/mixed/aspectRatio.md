# aspectRatio

##### Aspect ratio of a resolution

   aspectRatio  () { for i in `seq 1 1000 `; do awk "BEGIN {print $1/$i, $2/$i}"; done |grep -v "\."; }

##### Aspect ratio of a resolution

   aspectRatio  () { for i in `seq 1 1000 `; do awk "BEGIN {print $1/$i, $2/$i}"; done |grep -v "\."; }
