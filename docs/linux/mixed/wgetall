# wgetall

##### Download all files of a certain type with wget.

   wgetall  () { wget -r -l2 -nd -Nc -A.$@ $@ }

##### Download all files of a certain type with wget.

   wgetall  () { wget -r -l2 -nd -Nc -A.$@ $@ }
