# nocache

##### prevent large files from being cached in memory (backups!)

   nocache  <I/O-heavy-command>

##### prevent large files from being cached in memory (backups!)

   nocache  <I/O-heavy-command>
