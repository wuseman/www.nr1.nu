# w_scan

##### build DTT channel list with w-scan

   w_scan  -X -P -t 2 -E 0 -c IT > dvb-channels.conf

##### build DTT channel list with w-scan

   w_scan  -X -P -t 2 -E 0 -c IT > dvb-channels.conf
