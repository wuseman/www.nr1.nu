# zfs

##### Destroy all ZFS snapshots

   zfs  list -H -o name -t snapshot | xargs -n1 -t zfs destroy

##### Destroy all ZFS snapshots

   zfs  list -H -o name -t snapshot | xargs -n1 -t zfs destroy
